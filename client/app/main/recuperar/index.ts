'use strict';
const angular = require('angular');
import RecuperarController from './recuperar.controller';

export default angular.module('colmorovApp.recuperar', [])
  .controller('RecuperarController', RecuperarController)
  .name;